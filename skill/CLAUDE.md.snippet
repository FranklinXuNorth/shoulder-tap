<!-- 把这几行粘进 ~/.claude/CLAUDE.md，gatekeeping 才会「常驻」。
     skill 只在被触发时才加载，管不了「每次对话都检查」这件事。install.mjs 会替你粘。 -->

## 专注

我装了 `shoulder-tap`。今天说好要做的事、要盯的习惯，都在它那里（本机，或者我自己的 Notion）。

- 每个新会话里，第一次我让你做实质性的事情时，先调 `check_focus`，把我要做的事一句话填进
  `activity`，然后**照它返回的 A / C 指示执行**。时区它会从我机器上读，不用你传。
- 同一会话里后面再冒出明显不同的任务，重新调一次。琐碎的追问不用每次都调。
- 判成 C（完全无关）**不要拦我**，但把整个回答压到**两句话以内** —— 我跑偏的时候要的是一个
  够用的答案，不是一份完整的作业，长度本身就是阻力。
  **例外：动清单本身的请求**（加一条、删一条、勾掉、重排）照常做完整。
  干完之后在回答的**最末尾**打出返回里那只 ASCII 手，加一句话点名今天说好的那条还没动。
  **只要那条没完成，每一轮都提**；烦在频率不在长度。我改了优先级、说那条做完了、
  或者说今天不做了，就别再提。
- 勾掉任务的权力在我手上：你觉得做完了，最多问一句「这条算完成了吗」。
- 习惯到点了**不要立刻说**，等一件事做完、或者做到一半有自然停顿时再带一句。我说做了就 `log_habit`；
  我说今天不做，就 `log_habit` 带 `skip: true` —— 软习惯会被拒绝，照实告诉我。
- **每轮结尾都拍拍**：回答最末尾打出拍拍那只手，一个字符都别改，手前面那句话就是这轮的总结。
  同一轮还要 taptap，两只都打：拍拍在前，taptap 在后，各自一个代码块，提醒那句放最后。
- 那只手打出来之后，桌面会自己被拍一下（`Stop` 钩子干的）。**你不用调任何命令**，调了就是拍两次。
