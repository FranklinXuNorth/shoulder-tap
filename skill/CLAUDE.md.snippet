<!-- 把这几行粘进 ~/.claude/CLAUDE.md，gatekeeping 才会「常驻」。
     skill 只在被触发时才加载，管不了「每次对话都检查」这件事。 -->

## 专注

我装了 `shoulder-tap` 这个 MCP，用来盯住我今天说好要做的事。

- 每个新会话里，第一次我让你做实质性的事情时，先调用 `shoulder-tap` 的 `check_focus`，
  把我要做的事作为 `activity` 传进去，然后**按它返回的 A / B / C 指示执行**。
- 同一个会话里后面再冒出新的、明显不同的任务，再 call 一次。琐碎的追问不用每次都 call。
- 它说拦，就拦住问我，不要先帮我做了再说。这是我自己要求的。
