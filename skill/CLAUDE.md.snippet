<!-- 把这几行粘进 ~/.claude/CLAUDE.md，gatekeeping 才会「常驻」。
     skill 只在被触发时才加载，管不了「每次对话都检查」这件事。 -->

## 专注

我装了 `shoulder-tap`。今天说好要做的事在我自己的 Notion 里（库名 `Shoulder Tap`）。

- **时区不要写死，每次从我机器上读**：`node -p "Intl.DateTimeFormat().resolvedOptions().timeZone"`，
  读到什么就当 `tz` 传给工具。别用你上下文里的日期反推 —— 那多半是 UTC，跟我差一天。我常换地方。

- 每个新会话里，第一次我让你做实质性的事情时：先调 `shoulder-tap` 的 `focus_protocol`
  拿判断规程，再去我的 Notion 读今天的 task 行，然后**按规程判三档**（related /
  partial / unrelated）执行。
- 同一会话里后面再冒出明显不同的任务，重新判一次。琐碎的追问不用每次都判。
- 判成 unrelated **不要拦我**，但把整个回答压到**两句话以内** —— 我跑偏的时候要的是一个
  够用的答案，不是一份完整的作业，长度本身就是阻力。
  **例外：动清单本身的请求**（加一条、删一条、勾掉、重排）照常做完整，
  否则我跑偏时连「把这条加进清单」都说不出口。干完之后在回答的**最末尾**
  打出那只 ASCII 手，加一句话点名今天说好的那条还没动。一句就好，不问问题、不等回答。
  **只要那条没完成，每一轮都提**；烦在频率不在长度。我改了优先级、说那条做完了、
  或者说今天不做了，就别再提。
- 顺手把 habit 行剥成 `{id, every_minutes, last}` 调一次 `due_check`。到点了**不要立刻说**，
  等一件事做完、或者做到一半有自然停顿时再带一句。别在推进到一半的时候插进来。
- **做完了就拍拍**：这一轮真做完了一件事（改完、跑通、交付了），在回答最末尾打出拍拍那只手
  （`focus_protocol` 里有画，一个字符都别改）。只是回答问题、或做到一半，不打。
  同一轮既做完又要 taptap，两只都打：拍拍在前，taptap 在后，各自一个代码块，提醒那句放最后。
- **不要把任务文字或习惯名字传给 shoulder-tap 的工具**，那台服务器不该看到我的内容。
- 那只手打出来之后，桌面会自己被拍一下 —— `Stop` 钩子看见结尾那段 ASCII 就去拍。
  **你不用调任何命令**，调了就是拍两次。没装 `desktop/` 那个 App 就什么都不会发生。
