# 可选。设了就必须在 MCP 配置里带 X-Shoulder-Tap-Key 这个头，用来挡住路人蹭你的 Vercel 额度。
# 不设 = 谁都能连（但他们只能操作自己 Notion 里的东西，你的数据碰不到）。
SHOULDER_TAP_KEY=

# 服务器跑在 UTC。没传 day 参数时按这个时区算「今天」。中国是 8。
TIMEZONE_OFFSET_HOURS=8

# 第一步部署时留空 = 端点开放，只能调 ping，用来验证 MCP 通不通。
# 验证完了设成 on 再 Redeploy = 必须带 Notion token，全部工具可用。
SHOULDER_TAP_AUTH=

# 配了才会出现 classify_focus 工具：让 Jev（TypeSafe AI 的 System One 模型）几百毫秒判一档。
# 代价：调用时「用户现在要做什么」和「当前那条任务」这两行字会经过这台机器再到 TypeSafe AI。
# 不配 = 判断完全由调用方的模型在本地做，一个字都不出去。
JEV_API_KEY=
