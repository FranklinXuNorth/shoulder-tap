# 可选。设了就必须在 MCP 配置里带 X-Shoulder-Tap-Key 这个头，用来挡住路人蹭你的 Vercel 额度。
# 不设 = 谁都能连（但他们只能操作自己 Notion 里的东西，你的数据碰不到）。
SHOULDER_TAP_KEY=

# 调用方没传 tz 时的兜底时区，IANA 名字。猜错就会整整差一天，所以务必设成你自己的。
DEFAULT_TIMEZONE=America/New_York

# 一天从几点开始，默认 4。不是午夜 —— 23 点列的清单不该在你还没睡的时候翻页。
DAY_STARTS_AT_HOUR=
